@model IEnumerable<TuneMax.Models.News>

@{
    ViewBag.Title = "آرشیو";
}

<h2>آرشیو</h2>

<p class="create">
    @Html.ActionLink("ایجاد", "Create")
</p>
<table class="gv">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NewsGroup_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Account.Fullname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NewsGroup.Name
)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Account.Fullname)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.Id })
            @Html.ActionLink("جزئیات", "Details", new { id=item.Id })
            @Html.ActionLink("حذف", "Delete", new { id=item.Id })
        </td>
    </tr>
}
</table>
